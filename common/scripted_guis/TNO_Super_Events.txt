scripted_gui = {

	TNO_super_events = {
		context_type = player_context
		window_name = "TNO_SG_Super_Event"

		visible = {
			#always = yes
			OR = {
			has_variable = tno_se
			AND = {
				tag = ZZZ
				has_variable = tno_se
			}
			
			}
		}
		triggers = {
			underlay_visible = {
				NOT = { has_global_flag = TNO_my_world_is_on_fire_how_about_yours }
			}
		}
		properties = {
			image = { image = "[tnogetseimage]" }
		}
		effects = {
			option_click = {
				clear_variable = tno_se
			}
		}
	}
}
